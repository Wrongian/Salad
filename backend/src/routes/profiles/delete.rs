// use std::sync::Arc;

// use tide::{Request, Response};

// use crate::types::state::TideState;

// delete profile response builder
// pub fn delete_profile(req: Request<Arc<TideState>>) -> tide::Result {
//     // TODO: implementation
//     Ok(Response::builder(200).build())
// }
